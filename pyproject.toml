[project]
name = "hevyctl"
version = "0.1.0"
description = "CLI to interact with the Hevy API"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
hevyctl = "hevy.cli:main"

[tool.uv]
package = true